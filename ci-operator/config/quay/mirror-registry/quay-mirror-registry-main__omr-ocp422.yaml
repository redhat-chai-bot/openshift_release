base_images:
  upi-installer:
    name: "4.22"
    namespace: ocp
    tag: upi-installer
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9-release-golang-1.24-openshift-4.22
releases:
  latest:
    integration:
      name: "4.22"
      namespace: ocp
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: quay-omr-tests-omr-ocp422-disconnected
  cron: 0 0 * * 1
  steps:
    cluster_profile: aws-quay-qe
    env:
      BASE_DOMAIN: quayqe.devcluster.openshift.com
      MULTISTAGE_PARAM_OVERRIDE_OMR_IMAGE: quay.io/redhat-user-workloads/quay-eng-tenant/mirror-registry-v2-0:mirror-registry-v2-0-on-push-wjx8j-build-image-index
    post:
    - chain: quay-tests-aws-ipi-disconnected-private-deprovision
    test:
    - chain: quay-tests-aws-ipi-disconnected-private-provision
zz_generated_metadata:
  branch: main
  org: quay
  repo: mirror-registry
  variant: omr-ocp422
